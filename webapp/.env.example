# Optional: seed the workspace on first boot (can also be set in Settings → Sandbox)
E2B_API_KEY=
# Optional: require a password to access the workspace (recommended for cloud deploys)
WORKSPACE_PASSWORD=
# Optional: default provider settings seeded on first boot
DEFAULT_LLM_BASE_URL=https://agaam2-7dba6cfc4d0a.herokuapp.com
DEFAULT_LLM_API_KEY=
